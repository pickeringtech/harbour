APP_NAME=HarbourAcceptance
APP_ENV=local
APP_KEY=${APP_KEY}
APP_DEBUG=true
APP_URL=${APP_URL}

DB_CONNECTION=${DB_CONNECTION}
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_DATABASE=${DB_DATABASE}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}

REDIS_CLIENT=${REDIS_CLIENT}
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=${REDIS_PORT}
REDIS_PREFIX=${REDIS_PREFIX}
CACHE_STORE=redis
CACHE_PREFIX=${CACHE_PREFIX}
SESSION_DRIVER=redis
SESSION_COOKIE=${SESSION_COOKIE}
QUEUE_CONNECTION=redis
QUEUE_PREFIX=${QUEUE_PREFIX}
QUEUE_NAME=${QUEUE_NAME}
REDIS_QUEUE=${REDIS_QUEUE}
HORIZON_PREFIX=${HORIZON_PREFIX}

VITE_PORT=${VITE_PORT}
REVERB_HOST=127.0.0.1
REVERB_PORT=${REVERB_PORT}
REVERB_SERVER_HOST=127.0.0.1
REVERB_SERVER_PORT=${REVERB_PORT}
REVERB_APP_ID=harbour-acceptance
REVERB_APP_KEY=harbour-acceptance-key
REVERB_APP_SECRET=harbour-acceptance-secret
REVERB_SCHEME=http
VITE_REVERB_APP_KEY=harbour-acceptance-key
VITE_REVERB_HOST=127.0.0.1
VITE_REVERB_PORT=${REVERB_PORT}
VITE_REVERB_SCHEME=http
