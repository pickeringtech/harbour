# Team-owned equivalent configuration.
defaultTabs:
  - title: App
    command: composer workspace:dev
scripts:
  setup: composer install --no-interaction && composer workspace:setup
  archive: composer workspace:teardown -- --force
